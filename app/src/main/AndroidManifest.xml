<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chitranjank.apps.socialchats">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SocialChats">

        <activity android:name=".Utilities.HomePage" />
        <activity android:name=".Utilities.LoginAccount" />
        <activity android:name=".Utilities.CreateAccount" />
        <activity android:name=".UsersActivity"/>
        <activity android:name=".UserProfile"/>
        <activity android:name=".ChatActivity"/>

        <activity android:name=".Fragments.Options.ImageActivity" />
        <activity android:name=".Fragments.Options.MusicActivity" />
        <activity android:name=".Fragments.Options.WritePostsActivity" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>